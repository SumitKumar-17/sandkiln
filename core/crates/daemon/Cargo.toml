[package]
name = "sandkiln-daemon"
version = "0.0.1"
edition.workspace = true
license.workspace = true

[[bin]]
name = "sandkilnd"
path = "src/main.rs"

[dependencies]
sandkiln-protocol = { path = "../protocol" }
sandkiln-vmm = { path = "../vmm" }
sandkiln-store = { path = "../store" }
axum = { version = "0.7", features = ["ws"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros", "net", "time", "sync"] }
# For splitting/pumping a WebSocket (Stream + Sink) against an async
# UnixStream in routes_pty.rs -- already compiled in transitively via
# axum itself, naming it directly costs nothing new.
futures-util = "0.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter", "json"] }
tower-http = { version = "0.5", features = ["trace"] }
uuid = { version = "1", features = ["v4"] }
caps = "0.5"
# For encoding the credentials file `routes_mounts.rs` writes into the
# guest via `Request::WriteFile` -- every other caller of that request
# base64-encodes real *file* content on their own side and passes it
# through untouched; this is the first daemon-originated content that
# needs the same encoding done here instead. Same crate/API
# `sandkiln-guest-agent` already uses for the decode side.
base64 = "0.22"
# Already implicit in the dependency tree via axum 0.7 (which is built on
# hyper 1.x); these just turn on the client side of what's already there
# for the /preview reverse proxy, rather than pulling in a whole separate
# HTTP client stack (e.g. reqwest) for one internal hop to a guest's own
# port on the bridge network.
hyper = { version = "1", features = ["client", "http1"] }
hyper-util = { version = "0.1", features = ["client-legacy", "http1", "tokio"] }
