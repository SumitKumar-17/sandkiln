---
// Replaces Starlight's default SiteTitle so the docs header carries the same
// top-level nav as the marketing pages. Without this the docs are a one-way
// trip: the sidebar covers the docs tree, but nothing points back out to
// Architecture/Performance/Roadmap, which is the whole reason these were
// merged into one project.
const base = import.meta.env.BASE_URL.replace(/\/$/, "");
const link = (path: string) => (path === "" ? `${base}/` : `${base}/${path}/`);

const nav = [
  { label: "Architecture", path: "architecture" },
  { label: "Performance", path: "performance" },
  { label: "Roadmap", path: "roadmap" },
];
---

<div class="site-head">
  <a href={link("")} class="site-title sl-flex">
    <span translate="no">sandkiln</span><span class="dot" aria-hidden="true">.</span>
  </a>
  <nav class="cross-nav" aria-label="Site">
    {nav.map((item) => <a href={link(item.path)}>{item.label}</a>)}
  </nav>
</div>

<style>
  .site-head {
    display: flex;
    align-items: baseline;
    gap: 1.75rem;
    min-width: 0;
  }
  .site-title {
    align-items: baseline;
    font-family: var(--display);
    font-size: var(--sl-text-h4);
    font-weight: 800;
    letter-spacing: 0.01em;
    color: var(--sl-color-white);
    text-decoration: none;
    white-space: nowrap;
    min-width: 0;
  }
  .dot {
    color: var(--sl-color-accent);
  }
  .cross-nav {
    display: flex;
    gap: 1.25rem;
    font-size: var(--sl-text-sm);
  }
  .cross-nav a {
    color: var(--sl-color-gray-2);
    text-decoration: none;
    white-space: nowrap;
  }
  .cross-nav a:hover {
    color: var(--sl-color-white);
  }
  /* Starlight's header already competes for room with search and the theme
     toggle; below its own nav breakpoint the cross-links would wrap the bar
     onto a second line, so they drop out and the sidebar carries navigation. */
  @media (max-width: 72rem) {
    .cross-nav {
      display: none;
    }
  }
</style>
