---
import "../styles/global.css";

interface Props {
  title?: string;
  description?: string;
}

const {
  title = "sandkiln",
  description = "A compute primitive for safely running untrusted or AI-generated code — hardware-isolated Firecracker microVMs, real benchmarks, SDKs for JS/TS, Python, and a CLI.",
} = Astro.props;

// An amber plate carrying a block cursor: the site's two ideas — a serial
// console and one accent hue — at 16px.
const favicon =
  'data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32"><rect width="32" height="32" fill="%23f2870f"/><rect x="7" y="7" width="11" height="14" fill="%23150d02"/><rect x="7" y="24" width="18" height="3" fill="%23150d02"/></svg>';
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <meta name="color-scheme" content="light dark" />
  <link rel="icon" href={favicon} />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  {/* One family for the whole site, headlines included — see tokens.css. */}
  <link
    href="https://fonts.googleapis.com/css2?family=Azeret+Mono:wght@400;500;600;700&display=swap"
    rel="stylesheet"
  />
  {/* Starlight owns the theme toggle and persists the choice under its own
      localStorage key. These pages aren't rendered by Starlight, so without
      this they'd ignore that choice and snap back to the OS setting the
      moment a reader crossed from the docs — one site, two themes. Runs
      before first paint to avoid a flash of the wrong one. */}
  <script is:inline>
    try {
      const stored = localStorage.getItem("starlight-theme");
      if (stored === "dark" || stored === "light") {
        document.documentElement.dataset.theme = stored;
      }
    } catch {}
  </script>
</head>
<body>
  <slot />
</body>
</html>
